{
    "name": "meu-projeto/cadastro-clientes",
    "description": "Uma aplicação de cadastro de clientes usando PHP e MySQL",
    "require": {
        "php": ">=7.4",
        "ext-pdo": "*"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "php -r \"file_exists('.env') || copy('.env.example', '.env');\""
        ]
    }
}
